// ------------------------------------------------------------
// In this file we will have the functionality for the cards
// RenderCards()
// AddNewCard()
// RemoveOneCard()
// ------------------------------------------------------------

// Getting all the json data and creating a cards element
// const cardStack = document.getElementById("cardStack");

// dateData.forEach((card, i) => {
//   const cardElement = document.createElement("div");
//   cardElement.className = "card";
//   cardElement.innerText = `${card.category} of ${card.question}`;
//   cardStack.appendChild(cardElement);
// });